# Mobile package specific npm configuration
# Ensures NativeWind and its dependencies resolve correctly

# Legacy peer deps for React Native compatibility
legacy-peer-deps=true

# Auto-install peer dependencies
auto-install-peers=true

# Disable strict peer dependencies check
strict-peer-dependencies=false